# Keywords (deprecated)

This folder was used for Porcupine custom wake word files (`.ppn`). The project now uses **OpenWakeWord** exclusively, which has a built-in "hey jarvis" model—no keyword files needed. See [docs/OPENWAKEWORD.md](../../docs/OPENWAKEWORD.md).
